service: codeverifiers

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  memorySize: 512
  timeout: 20
  
  stage: dev
  region: ap-southeast-1

functions:
  productCodes:
    handler: handler.productCodes
    events:
    - http:
        path: /codes-to-verifier
        method: get
    runtime: nodejs18.x
    memorySize: 512
    timeout: 20
